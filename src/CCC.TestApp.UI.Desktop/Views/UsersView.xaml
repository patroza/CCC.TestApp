<UserControl x:Class="CCC.TestApp.UI.Desktop.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:entities="clr-namespace:CCC.TestApp.Core.Domain.Entities;assembly=CCC.TestApp.Core.Domain"
             xmlns:viewModels="clr-namespace:CCC.TestApp.UI.Desktop.ViewModels"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:UsersViewModel}" cal:Bind.AtDesignTime="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="Users" cal:Message.Attach="[Event MouseDoubleClick] = [Action ShowSelectedUser()]">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type entities:User}">
                    <TextBlock Text="{Binding UserName}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>