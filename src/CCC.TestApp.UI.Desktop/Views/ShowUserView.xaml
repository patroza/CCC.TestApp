<UserControl x:Class="CCC.TestApp.UI.Desktop.Views.ShowUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:CCC.TestApp.UI.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewModels:ShowUserViewModel}">
    <Grid>
        <DockPanel VerticalAlignment="Top">
            <TextBlock Text="{Binding User.UserName}" DockPanel.Dock="Top" />
            <TextBlock Text="{Binding User.Id}" DockPanel.Dock="Top" />
            <Button Content="Edit" x:Name="Edit" DockPanel.Dock="Top" />
            <Button Content="Destroy" x:Name="Destroy" DockPanel.Dock="Top" />
        </DockPanel>
    </Grid>
</UserControl>